<div class="row justify-content-center">
  <h1>Bem-vindos ao universo de Star Wars!</h1>
</div>
<div class="row justify-content-center">
  <% @people.each do |person| %>
    <div class="card-person m-3 col-12 col-md-4 col-lg-3">
      <div class="card-person-infos">
        <h2><%= link_to person.name, person_path(person) %></h2>
      </div>
    </div>
  <% end %>
</div>
