<div class="row justify-content-center">
  <h1>Welcome to the Star Wars universe!</h1>
</div>
<div class="row justify-content-center">
  <% @people.each do |person| %>
    <div class="card-person m-3 col-10 col-md-3 col-lg-2">
      <div class="card-person-infos">
        <h2><%= link_to person.name, person_path(person) %></h2>
      </div>
    </div>
  <% end %>
</div>
